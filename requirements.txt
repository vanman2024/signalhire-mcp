# SignalHire MCP Server — runtime dependencies
#
# Kept in sync with pyproject.toml, which is the source of truth. This file
# exists for `pip install -r` on hosts without uv.
#
# Every pin is exact. fastmcp 4 is a prerelease, and pip/uv resolution that
# allows prereleases will happily pull pydantic 2.14.0b1 and httpx 1.0.dev3 for
# any loosely-specified package. Exact pins keep the prerelease surface to
# fastmcp alone.
#
# FastMCP 4 uses httpx2, NOT httpx. Do not add httpx back — FastMCP raises
# httpx2 exceptions, so `except httpx.ConnectError` becomes dead code that
# silently stops catching anything.
#
# Install with:  uv pip install --prerelease=allow -r requirements.txt
#            or: pip install --pre -r requirements.txt

fastmcp==4.0.0b2
httpx2==2.10.0
pydantic==2.13.4
pydantic-settings==2.15.0
python-dotenv==1.2.2

# Testing (install with: uv pip install --prerelease=allow -r requirements.txt)
pytest==9.1.1
pytest-asyncio==1.4.0
